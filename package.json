{
	"name": "branchops-ai-intake-worker",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"build": "tsc --noEmit",
		"deploy:dry-run": "wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest run",
		"validate": "npm run build && npm run test",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.6.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.81.1"
	}
}
